{
  "name": "petplatform",
  "version": "0.1.0",
  "private": true,
  "description": "Платформа для питомцев (Узбекистан) — монорепо",
  "workspaces": [
    "services/*",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm start --workspace services/api",
    "dev": "npm run dev --workspace services/api",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
